3.
Compute metrics / powerlaw / dataaccessor:
- Bin into histogram inside kfac code (not abg because
those can be computed after). default=1024 bins.
Normalised to a density. Store log and not log. Also
store something to convert back to counts as well (if
this is needed, maybe it's not idk. or maybe it's
better to store in counts if it's easier to normalise
after the fact).
- Add size of mean to the metrics (everywhere there is
a mean (so like a/b/g))
- Add dimension of data everywhere (kfac and abg) to
the metrics for convenience.

4.
Test on alternative datasets (memorization, quotes,
maths), to obtain conditional statistics.

5.
histogram of eigenspectra, note: probably better to
have axis show unnormalised. To get unnormalised,
just multiply by trace.

6.
Add spectral statistics of the weights themselves.
Note: this is ideally a different run and not stored in
the others, otherwise it would just be a duplicate
for each and every run, since it isn't data-dependent.

Then we can reproduce
https://arxiv.org/abs/2410.17770.
But then I want to basically invert the roles. So
we plot the maximum overlap between activation
eigenvectors and matrix singular vectors but along the
axis of the histogram of activations instead of weights

7.
Get OV data.
